services:
  - type: web
    name: tags-furlong
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --timeout 120
    envVars:
      - key: SECRET_KEY
        generateValue: true
      # Conexion PostgreSQL (Supabase, Session pooler). Se define aca para que Render
      # la aplique automaticamente al servicio en cada deploy (rol de app dedicado).
      - key: DATABASE_URL
        value: postgresql://furlong_app.mssmzevutlgfqfwuntkp:Frlg7Tags2026Xq9Pd@aws-0-sa-east-1.pooler.supabase.com:5432/postgres
